//= link_tree ../images
//= link_tree ../builds

/*
 * Tailwind CSSとは別に独自のCSSファイルを追加したい場合は、
 * 以下の行のコメントを外して `app/assets/stylesheets` ディレクトリを含めてください。
 * カスタムCSSファイルは必ず `app/assets/stylesheets` に配置してください。
 */
// = link_tree ../stylesheets
